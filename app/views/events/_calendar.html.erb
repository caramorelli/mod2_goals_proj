

<%= month_calendar events: events do |date, events| %>
  <%= date.strftime('%d') %>

  <% events.each do |event| %>
    <div class='event_label'>

      <b face='Fascinate Inline' color='black'>Completed:</b> <%=link_to event.name, goals_path %>
    </div>
  <% end %>
<% end %>
